% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ablation.R
\name{ablation-targets-api}
\alias{ablation_prepare_representation_inputs}
\alias{ablation_run_representation_nodes}
\alias{ablation_run_representation_node}
\alias{ablation_make_learning_curve_jobs}
\alias{ablation_combine_learning_curve_jobs}
\alias{ablation_make_scaling_jobs}
\alias{ablation_runtime_metadata}
\title{Targets-facing CCS ablation calculation API}
\description{
Pure calculation adapters used by the ablation-03 targets project. They do not
create targets stores, SUCCESS markers, or stage receipts; persistence and
invalidation are owned by targets.
}
\usage{
ablation_prepare_representation_inputs(object, data, metadata = NULL,
  params = list(), seed = 20260727, cache_dir = NULL, verbose = FALSE)
ablation_run_representation_nodes(inputs, seed = NULL, verbose = FALSE)
ablation_run_representation_node(inputs,
  node = c("native_geometry", "retrieval", "readout", "learning_curve", "scaling", "decoder"),
  seed = NULL, verbose = FALSE)
ablation_make_learning_curve_jobs(inputs)
ablation_combine_learning_curve_jobs(jobs, results)
ablation_make_scaling_jobs(inputs)
ablation_runtime_metadata(cache_root,
  store = file.path(cache_root, "targets"),
  run_id = "manual")
}
\arguments{
\item{object}{A \code{CCS} object containing the frozen d1 representation.}
\item{data}{Raw expression data accepted by \code{ablation()}.}
\item{metadata}{Optional sample metadata.}
\item{params}{Representation parameter overrides.}
\item{seed}{Master random seed.}
\item{cache_dir}{Optional Direct feature cache directory. \code{NULL} keeps
the preparation in memory.}
\item{verbose}{Whether to report progress.}
\item{inputs}{A \code{CCSRepresentationInputs} object.}
\item{node}{Representation node to select.}
\item{jobs}{Deterministic job table.}
\item{results}{List of job result rows.}
\item{cache_root}{Formal ablation cache root.}
\item{store}{Targets store path.}
\item{run_id}{Run identifier recorded in runtime metadata.}
}
\value{
\code{ablation_prepare_representation_inputs()} returns a
\code{CCSRepresentationInputs} object; node functions return calculation
results or deterministic job tables; \code{ablation_runtime_metadata()} returns
a runtime identity list.
}
