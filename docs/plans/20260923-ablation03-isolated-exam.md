# ablation-03 隔离小样本验收

## 问题与影响

正式分析和测试如果共用运行状态，就像用同一本账簿记录两次实验，无法证明测试独立跑通。先排除 R 依赖及调度问题，再用真实数据子集验证完整报告链路。

## 目标与边界

- 在 test/ablation-03/tmp/exam-20260923-01 留存约千例 reference、千例 query 的输入、代码副本、独立 cache 和验收记录。
- 通过同一 _targets.R 图执行计算、资源观测和四份 HTML；从空测试 store 起步，绝不共用正式缓存。
- 测试成功后才调整正式入口；不启动正式分析、不修改包版本或科学参数。

## 实施方向

停止确认属于主分析的进程，审计 renv 锁文件与 targets/crew 入口，检查输入结构；保留未提交改动制作独立副本及子集。依据第一个失败节点定位根因并最小修复，重跑整图并验证 HTML；成功后将已验证改动带回正式项目。

## 验收与风险

- 已完成隔离的 16 target、四份 HTML；正式输入自覆盖已消除，正式图仅通过 manifest 静态检查，未启动正式计算。
- 本机 renv 状态仍提示三个本地包来源未知；正式输入含预计算生物/结构字段，小测重新计算这些字段，正式分支仍需审查。

- 测试 cache、targets store、资源日志完全留在 exam 目录；正式流程未运行。
- 全部 target 成功，四份 HTML 非空且可读取；记录真实样本量、失败/成功证据并校验 BAC。
- 数据不完整或资源不足时不得用旧产物、减少科学参数或跳过阶段假装通过；此时不改正式入口。
